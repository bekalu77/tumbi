services:
  - type: web
    name: tumbi
    env: node
    plan: free
    healthCheckPath: /api/health
    buildCommand: npm install --also=dev && cd client && npm install && cd .. && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
      - fromGroup: tumbi-secrets
